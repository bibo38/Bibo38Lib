apply plugin: 'java'
apply plugin: 'eclipse'

version = 1.2

sourceSets {
	main {
		java {
			srcDir 'src'
		}
		resources {
			srcDir 'src'
		}
	}
}

dependencies {
	compile fileTree(dir: 'libs', include: '*.jar')
}

task wrapper(type: Wrapper) {
	gradleVersion = '2.4'
}
